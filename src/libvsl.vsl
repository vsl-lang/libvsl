@backend(llvm)
private func puts(string: CString) -> Int32 external(puts)

@backend(llvm)
@mock(opaquepointer) private class CFile {}

@backend(llvm)
private func fdopen(fd: Int32, mode: ByteSequence) -> CFile external(fdopen);

@backend(llvm)
private func fflush(stream: CFile) -> Void external(fflush);
